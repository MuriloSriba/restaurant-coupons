# Configurações de Produção
PORT=3001
NODE_ENV=production

# JWT Secret (gerar uma chave forte)
JWT_SECRET=sua_chave_secreta_aqui_muito_segura

# Database (SQLite já está configurado)
DB_PATH=./database.sqlite
